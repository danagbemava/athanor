# Athanor Spec Package (Scaffold)

Canonical shared data contracts for API, worker, and UI.

## Contents

- `schemas/`: JSON Schema placeholders (versioned)
- `fixtures/`: valid/invalid payload examples per schema
- `golden/`: golden determinism scenario corpus

## Notes

Schemas are scaffold-level and intentionally minimal. They define contract boundaries, not final domain behavior.
